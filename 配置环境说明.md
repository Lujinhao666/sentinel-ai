# Sentinel AI — 部署环境说明书

## 1. 项目概述

| 项目 | 说明 |
|------|------|
| 项目名称 | Sentinel AI |
| 项目类型 | 官网首页 |
| 技术架构 | SPA (Single Page Application) |
| 主要功能 | 首页 3D 立体动态场景展示、企业服务介绍 |
| 当前版本 | 0.0.1 |

---

## 2. 技术栈清单

### 2.1 核心框架

| 依赖包 | 版本要求 | 用途 |
|--------|----------|------|
| React | ^18.3.1 | UI 组件库，函数式开发 |
| React DOM | ^18.3.1 | React DOM 渲染引擎 |
| Vite | ^5.4.10 | 下一代前端构建工具 |
| TypeScript | ^5.6.3 | 静态类型检查 |

### 2.2 UI & 样式

| 依赖包 | 版本要求 | 用途 |
|--------|----------|------|
| Tailwind CSS | ^3.4.14 | 原子化 CSS 框架 |
| tailwindcss-animate | ^1.0.7 | Tailwind CSS 动画扩展 |
| autoprefixer | ^10.4.20 | CSS 前缀自动补全 |
| postcss | ^8.4.47 | CSS 转换工具 |

### 2.3 UI 组件库

| 依赖包 | 版本要求 | 用途 |
|--------|----------|------|
| @radix-ui/react-slot | ^1.1.0 | 可组合 Slot 组件 |
| class-variance-authority | ^0.7.0 | 组件变体管理 |
| clsx | ^2.1.1 | 类名条件拼接 |
| tailwind-merge | ^2.5.4 | Tailwind 类名智能合并 |
| lucide-react | ^0.451.0 | 图标库 |

### 2.4 3D & 交互

| 依赖包 | 版本要求 | 用途 |
|--------|----------|------|
| @splinetool/react-spline | ^4.0.3 | Spline 3D 场景 React 组件 |
| @splinetool/runtime | ^1.1.0 | Spline 3D 运行时引擎 |

### 2.5 开发工具依赖

| 依赖包 | 版本要求 | 用途 |
|--------|----------|------|
| @vitejs/plugin-react | ^4.3.3 | Vite React 官方插件 |
| @types/node | ^22.7.5 | Node.js 类型定义 |
| @types/react | ^18.3.12 | React 类型定义 |
| @types/react-dom | ^18.3.1 | React DOM 类型定义 |

---

## 3. 系统环境要求

### 3.1 运行环境

| 组件 | 最低版本 | 推荐版本 | 说明 |
|------|----------|----------|------|
| Node.js | 18.0.0 | 20.x LTS 或更高 | JavaScript 运行时环境 |
| npm | 9.0.0 | 10.x LTS 或更高 | 包管理工具 |
| 操作系统 | Windows 10 / macOS 11 / Ubuntu 20.04 | 最新稳定版 | 支持跨平台部署 |

### 3.2 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|--------|----------|------|
| Chrome / Edge | 90+ | 推荐主用浏览器 |
| Firefox | 88+ | 完整支持 |
| Safari | 14+ | 部分 CSS 动画可能降级 |
| 移动端浏览器 | iOS Safari 14+ / Android Chrome 90+ | 响应式适配 |

### 3.3 服务器要求

| 资源 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 1 核 | 2 核或更高 |
| 内存 | 512 MB | 1 GB 或更高 |
| 磁盘空间 | 1 GB | 5 GB 或更高 |
| 网络带宽 | 1 Mbps | 10 Mbps 或更高 |

---

## 4. 项目结构

```
sentinel-ai/
├── index.html                          # 应用入口 HTML
├── package.json                        # 依赖与脚本配置
├── postcss.config.js                   # PostCSS 配置
├── tailwind.config.js                  # Tailwind CSS 配置
├── tsconfig.json                       # TypeScript 配置
├── tsconfig.node.json                  # Node.js TypeScript 配置
├── vite.config.ts                      # Vite 构建配置
├── dist/                               # 构建输出目录 (自动生成)
├── node_modules/                       # 依赖包目录 (自动生成)
└── src/
    ├── main.tsx                        # React 应用入口
    ├── App.tsx                         # 根组件
    ├── index.css                       # 全局样式与 Tailwind 指令
    ├── lib/
    │   └── utils.ts                    # 工具函数 (cn 类名合并)
    └── components/
        ├── Navbar.tsx                  # 顶部导航组件
        ├── HeroSection.tsx             # 首屏 3D 英雄区组件
        └── ui/
            └── button.tsx              # 通用按钮组件
```

---

## 5. 部署流程

### 5.1 环境准备

```bash
# 1. 确认 Node.js 版本
node -v    # 需 >= 18.0.0

# 2. 确认 npm 版本
npm -v     # 需 >= 9.0.0
```

### 5.2 获取代码

```bash
# 克隆项目仓库
git clone <repository-url> sentinel-ai

# 进入项目目录
cd sentinel-ai
```

### 5.3 安装依赖

```bash
# 使用 npm 安装全部依赖
npm install

# 如需使用国内镜像加速（可选）
npm install --registry=https://registry.npmmirror.com
```

### 5.4 开发模式运行

```bash
# 启动开发服务器 (带 HMR 热更新)
npm run dev

# 访问地址
# http://localhost:5173/

# 开发服务器配置
# host: localhost
# port: 5173
# 热更新: 已启用
```

### 5.5 生产环境构建

```bash
# 执行 TypeScript 类型检查 + Vite 生产构建
npm run build

# 构建产物输出到 dist/ 目录
# 构建产物包含：
#   index.html          # 入口 HTML 文件
#   assets/             # JS/CSS/静态资源
#   - 文件名带哈希值    # 支持长期缓存
```

### 5.6 本地预览生产构建

```bash
# 在本地预览生产构建产物
npm run preview

# 默认预览地址
# http://localhost:4173/
```

---

## 6. 服务器部署方案

### 方案 A：Nginx 静态部署（推荐）

```nginx
# /etc/nginx/conf.d/sentinel-ai.conf

server {
    listen 80;
    server_name your-domain.com;
    root /var/www/sentinel-ai/dist;
    index index.html;

    # SPA 路由支持 —— 所有请求回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存策略（带哈希文件名）
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;
}
```

**部署步骤：**

```bash
# 1. 在服务器上安装 Nginx
# Ubuntu/Debian
sudo apt install nginx -y

# CentOS/RHEL
sudo yum install nginx -y

# 2. 构建生产产物
npm run build

# 3. 部署产物到服务器
sudo cp -r dist/* /var/www/sentinel-ai/dist/

# 4. 配置 Nginx
sudo nano /etc/nginx/conf.d/sentinel-ai.conf

# 5. 测试并重启 Nginx
sudo nginx -t
sudo systemctl reload nginx
```

### 方案 B：Vercel / Netlify 平台部署

**Vercel 部署：**

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录 Vercel 账户
vercel login

# 在项目目录执行部署
cd sentinel-ai
vercel

# 生产环境部署
vercel --prod
```

**Netlify 部署：**

```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录 Netlify 账户
netlify login

# 构建并部署
cd sentinel-ai
netlify deploy --build --prod
```

### 方案 C：Docker 容器化部署

**Dockerfile：**

```dockerfile
# 构建阶段
FROM node:20-alpine AS builder
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# 运行阶段
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**docker-compose.yml：**

```yaml
version: "3.8"
services:
  sentinel-ai:
    build: .
    container_name: sentinel-ai
    ports:
      - "8080:80"
    restart: unless-stopped
```

**部署命令：**

```bash
# 构建镜像
docker build -t sentinel-ai:latest .

# 运行容器
docker run -d -p 8080:80 --name sentinel-ai sentinel-ai:latest

# 或使用 docker compose
docker compose up -d
```

---

## 7. 路径与配置说明

### 7.1 路径别名

```typescript
// vite.config.ts
resolve: {
  alias: {
    "@": path.resolve(__dirname, "./src")
  }
}

// tsconfig.json
"paths": {
  "@/*": ["./src/*"]
}
```

**使用示例：**

```typescript
import Navbar from "@/components/Navbar"       // src/components/Navbar
import { cn } from "@/lib/utils"                // src/lib/utils
```

### 7.2 环境变量

当前项目为纯前端静态站点，**无强制环境变量需求**。

如需扩展环境变量支持，可创建 `.env` 文件：

```env
# .env.production (仅示例，当前未使用)
VITE_API_BASE_URL=https://api.example.com
```

### 7.3 主题色变量

项目使用 CSS 自定义属性管理主题配色（定义于 `src/index.css`）：

| 变量名 | HSL 值 | 用途 |
|--------|--------|------|
| `--background` | 0 0% 10% | 页面背景 |
| `--foreground` | 0 0% 96% | 主要文字 |
| `--primary` | 119 99% 46% | 主色调 (翠绿) |
| `--secondary` | 0 0% 18% | 次要背景 |
| `--muted` | 0 0% 16% | 弱化背景 |
| `--accent` | 119 99% 46% | 强调色 |
| `--border` | 0 0% 20% | 边框色 |
| `--ring` | 119 99% 46% | 焦点环 |
| `--hero-bg` | 0 0% 8% | Hero 区背景 |

---

## 8. 外部依赖资源

### 8.1 Spline 3D 场景

- **资源地址**: `https://prod.spline.design/Slk6b8kz3LRlKiyk/scene.splinecode`
- **加载方式**: 异步懒加载（`React.lazy` + `Suspense`）
- **注意**: 需公网访问 Spline CDN，离线场景需自行托管 `.splinecode` 文件

### 8.2 Google Fonts

- **字体**: Sora (300-700)
- **加载方式**: `<link>` 预连接 + 样式表引入
- **注意**: 如需离线部署，需自行托管字体文件

---

## 9. 常见问题排查

### Q1: `npm install` 速度慢或超时

```bash
# 切换到国内镜像源
npm config set registry https://registry.npmmirror.com

# 或临时使用镜像
npm install --registry=https://registry.npmmirror.com
```

### Q2: PowerShell 禁止运行 npm.ps1 脚本

```powershell
# PowerShell 5.1 执行策略限制
# 解决方案一：使用 cmd 执行
cmd /c npm run dev

# 解决方案二：修改执行策略（需管理员权限）
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q3: 构建产物运行后页面空白

```bash
# 检查是否缺少 SPA 回退配置
# Nginx 需确保配置了：
# try_files $uri $uri/ /index.html;

# 检查构建产物完整性
ls -la dist/
# 应包含: index.html 和 assets/ 目录
```

### Q4: Spline 3D 场景无法加载

```bash
# 检查网络连通性
curl -I https://prod.spline.design/Slk6b8kz3LRlKiyk/scene.splinecode

# 若为内网环境，需将 .splinecode 文件下载到本地并修改 HeroSection.tsx 中的 scene URL
```

### Q5: TypeScript 类型检查错误

```bash
# 查看详细类型错误
npx tsc --noEmit

# 跳过类型检查（不推荐生产环境）
# 修改 vite.config.ts 启用 skipLibCheck
```

### Q6: 端口被占用

```bash
# 开发服务器指定端口
npm run dev -- --port 3000

# 生产预览指定端口
npm run preview -- --port 3000
```

---

## 10. 脚本命令汇总

| 命令 | 说明 |
|------|------|
| `npm install` | 安装项目全部依赖 |
| `npm run dev` | 启动 Vite 开发服务器 (默认端口 5173) |
| `npm run build` | 执行 TypeScript 类型检查 + 生产环境构建 |
| `npm run preview` | 本地预览生产构建产物 (默认端口 4173) |

---

## 附录

### A. 版本锁定

依赖版本采用 `^` 前缀（兼容次版本更新），生产环境部署前建议锁定版本：

```bash
# 使用 package-lock.json 精确安装
npm ci

# 或锁定所有依赖版本
npx npm-check-updates -l
```

### B. 安全建议

- 定期执行 `npm audit` 检查依赖漏洞
- 生产环境启用 HTTPS（Let's Encrypt 免费证书）
- Nginx 配置安全响应头（CSP、HSTS 等）
- 关闭不必要的端口和服务

### C. 扩展建议

- 如需 SSR（服务端渲染），可考虑迁移至 Next.js
- 如需更多 3D 交互，可扩展 Three.js 或 React Three Fiber
- 如需 CMS 内容管理，可集成 Sanity / Strapi